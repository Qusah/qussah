{#
| Variable                | Type   | Description                          |
|-------------------------|--------|--------------------------------------|
| component               | object | merchant settings from twilight.json |
| component.heading       | string | Main hero headline                   |
| component.subheading    | string | Supporting paragraph                 |
| component.image         | string | Hero product image                   |
| component.button_text   | string | CTA button label                     |
| component.button_url    | string | CTA button link                      |
| position                | int    | sorting block                        |
#}
<section class="s-block qissa-hero" id="qissa-hero-{{ position }}">
    <div class="container">
        <div class="qissa-hero__inner">
            {# Text side (right in RTL) #}
            <div class="qissa-hero__content">
                {% if component.heading %}
                    <h1 class="qissa-hero__title">{{ component.heading }}</h1>
                {% endif %}
                {# desktop description — merchant subheading (falls back to the brand description) #}
                <p class="qissa-hero__subtitle qissa-hero__subtitle--desktop">{{ component.subheading|default('متجر قُصة وجهتك الأولى لمستلزمات النظافة المنزلية — مناديل، صابون، معطرات وأكثر، باختيار سهل وتوصيل سريع من المنــاديل للمعطـــرات، كل ما تحتاجـــــه لبيت نظيف ومرتب — بجودة عالية وأسعار مباشرة من المصنع') }}</p>
                {# mobile tagline — fixed short line per Figma 6036:21489 #}
                <p class="qissa-hero__subtitle qissa-hero__subtitle--mobile">{{ trans('blocks.qissa.cleaning_tagline') }}</p>
                {% if component.button_text %}
                    {# Figma node 100:127 — order: arrow_left (left) · text · cart (right) #}
                    {# fall back to the products listing (same target as the header "المنتجات") #}
                    <a href="{{ (component.button_url and component.button_url not in ['#', '/products']) ? component.button_url : store.url ~ '/كل-المنتجات/c1390845573' }}" class="qissa-hero__btn">
                        <i class="sicon-arrow-left qissa-hero__btn-arrow"></i>
                        <span>{{ component.button_text }}</span>
                        <i class="sicon-shopping-cart qissa-hero__btn-cart"></i>
                    </a>
                {% endif %}
            </div>

            {# Image side (left in RTL) #}
            {% if component.image %}
                <div class="qissa-hero__media">
                    <img src="{{ component.image }}" alt="{{ component.heading }}" loading="eager" class="qissa-hero__img"/>
                </div>
            {% endif %}
        </div>
    </div>
</section>
